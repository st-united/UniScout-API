export const PARAMETER_KEY = {
  EXPIRED_GPTL: 'EXPIRED_GPTL',
  STUDENTS_PER_VEHICLE: 'STUDENTS_PER_VEHICLE',
  STUDENTS_PER_TEACHER: 'STUDENTS_PER_TEACHER',
  NEXT_OPENING_DAY: 'NEXT_OPENING_DAY',
};

export const roundedCeil = (number: number) => {
  return Math.ceil(number);
};
